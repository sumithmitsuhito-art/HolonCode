# C Language Skills Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Create two C language skills for HolonCode: c-tutor (闯关式教学 with progress tracking) and c-qa (答疑/代码审查).

**Architecture:** Two independent skill directories. c-tutor has a SKILL.md with embedded level content plus a plugin.py for progress persistence (3 tools). c-qa is a pure SKILL.md with no code. Both follow existing skill conventions (YAML frontmatter, role-switch/behavior/end-conditions sections).

**Tech Stack:** Python 3.12+, `@tool` decorator from skill_loader.py, JSON file in `data/` for progress storage, pytest for tests.

---

### Task 1: Create c-tutor plugin.py with progress management tools

**Files:**
- Create: `skills/c-tutor/plugin.py`
- Create: `tests/test_c_tutor_plugin.py`

- [ ] **Step 1: Create the skills/c-tutor directory**

```bash
mkdir -p D:/ClaudeCodeWorkSpace/DSPark-Code/skills/c-tutor
```

- [ ] **Step 2: Write the failing test for load_progress (no progress file yet)**

Create `tests/test_c_tutor_plugin.py`:

```python
"""Tests for c-tutor plugin progress management tools."""
import json
import sys
from pathlib import Path

# Ensure atri is importable
sys.path.insert(0, str(Path(__file__).resolve().parent.parent / "src"))

import pytest
from atri import DATA_DIR

PROGRESS_FILE = DATA_DIR / "c-tutor-progress.json"


@pytest.fixture(autouse=True)
def clean_progress():
    """Remove progress file before each test so tests are isolated."""
    if PROGRESS_FILE.exists():
        PROGRESS_FILE.unlink()
    yield
    if PROGRESS_FILE.exists():
        PROGRESS_FILE.unlink()


def test_load_progress_returns_default_when_no_file():
    from skills.c_tutor.plugin import load_progress
    result = load_progress()
    assert result["total_score"] == 0
    assert result["total_levels"] == 10
    assert result["completed_levels"] == 0
    assert result["levels"] == {}
```

- [ ] **Step 3: Run test to verify it fails**

Run: `cd D:/ClaudeCodeWorkSpace/DSPark-Code && uv run pytest tests/test_c_tutor_plugin.py::test_load_progress_returns_default_when_no_file -v`
Expected: FAIL (module `skills.c_tutor.plugin` not found)

- [ ] **Step 4: Write skills/c-tutor/plugin.py with load_progress**

```python
"""C-tutor progress management: load/save/reset learning progress.

Progress is stored in data/c-tutor-progress.json.
"""
import json
from datetime import date
from atri import DATA_DIR

PROGRESS_FILE = DATA_DIR / "c-tutor-progress.json"


def _default_progress() -> dict:
    return {
        "total_score": 0,
        "total_levels": 10,
        "completed_levels": 0,
        "levels": {},
    }


def _read_progress() -> dict:
    if not PROGRESS_FILE.exists():
        return _default_progress()
    try:
        return json.loads(PROGRESS_FILE.read_text(encoding="utf-8"))
    except (json.JSONDecodeError, OSError):
        return _default_progress()


def _write_progress(data: dict):
    PROGRESS_FILE.parent.mkdir(parents=True, exist_ok=True)
    tmp = PROGRESS_FILE.with_suffix(".tmp")
    tmp.write_text(json.dumps(data, ensure_ascii=False, indent=2), encoding="utf-8")
    tmp.replace(PROGRESS_FILE)


def load_progress():
    """读取C语言闯关学习的当前进度，包括总分、完成关卡数、各关卡状态。"""
    return _read_progress()
```

Note: `@tool` decorator is auto-injected by `SkillLoader.load_plugin()` — see `skill_loader.py:253`. We don't import or apply it in plugin.py directly; SkillLoader does `module.tool = tool` before exec.

However, `extract_tools()` looks for `_is_tool = True` attribute. Let me check how this works...

Looking at `skill_loader.py:254`, `spec.loader.exec_module(module)` runs the module code. But the `@tool` decorator is set as `module.tool = tool` BEFORE `exec_module`. So the module code can use `tool` as a bare name. But the functions defined in the module DON'T have `@tool` applied unless we explicitly decorate them.

Wait, I need to re-read the code. In `load_plugin()`:
```python
module.tool = tool  # line 253: inject @tool into module namespace
spec.loader.exec_module(module)  # line 254: execute the module
```

So when the module executes, `tool` is available as a global. If we write `@tool` above function definitions, they'll be decorated.

But the test imports the module directly (not through SkillLoader), so `tool` won't be defined. We need to handle this.

One approach: in plugin.py, try to import tool, fall back to a no-op decorator:

```python
try:
    from atri.skill_loader import tool
except ImportError:
    def tool(func=None, **kwargs):
        if func is None:
            return lambda f: f
        return func
```

This way tests can import directly and tools work without the decorator, while at runtime SkillLoader provides the real decorator.

Actually wait, looking more carefully: SkillLoader does `module.tool = tool` BEFORE `exec_module`. So inside plugin.py, `tool` is defined at module level. But when pytest imports it, `tool` won't be there unless we define a fallback.

Let me use the try/except pattern.
```

- [ ] **Step 5: Run the test to verify it passes**

Run: `cd D:/ClaudeCodeWorkSpace/DSPark-Code && uv run pytest tests/test_c_tutor_plugin.py::test_load_progress_returns_default_when_no_file -v`
Expected: PASS

- [ ] **Step 6: Write tests for save_progress**

Add to `tests/test_c_tutor_plugin.py`:

```python
def test_save_progress_creates_record():
    from skills.c_tutor.plugin import save_progress, load_progress
    save_progress("01", "变量与数据类型", 85)
    data = load_progress()
    assert data["completed_levels"] == 1
    assert data["total_score"] == 85
    assert data["levels"]["01"]["summary"] == "变量与数据类型"
    assert data["levels"]["01"]["score"] == 85
    assert data["levels"]["01"]["completed"] is True
    assert data["levels"]["01"]["completed_at"] is not None


def test_save_progress_updates_existing_level():
    from skills.c_tutor.plugin import save_progress, load_progress
    save_progress("01", "变量", 60)
    save_progress("01", "变量与数据类型", 90)
    data = load_progress()
    assert data["completed_levels"] == 1
    assert data["total_score"] == 90
    assert data["levels"]["01"]["score"] == 90


def test_save_progress_accumulates_multiple_levels():
    from skills.c_tutor.plugin import save_progress, load_progress
    save_progress("01", "变量", 80)
    save_progress("02", "分支", 90)
    save_progress("03", "循环", 70)
    data = load_progress()
    assert data["completed_levels"] == 3
    assert data["total_score"] == 240
    assert len(data["levels"]) == 3
```

- [ ] **Step 7: Run the tests to verify they fail**

Run: `cd D:/ClaudeCodeWorkSpace/DSPark-Code && uv run pytest tests/test_c_tutor_plugin.py::test_save_progress_creates_record tests/test_c_tutor_plugin.py::test_save_progress_updates_existing_level tests/test_c_tutor_plugin.py::test_save_progress_accumulates_multiple_levels -v`
Expected: FAIL (save_progress not defined)

- [ ] **Step 8: Add save_progress to plugin.py**

Append to `skills/c-tutor/plugin.py`:

```python
def save_progress(level_id: str, summary: str, score: int):
    """保存某一关的学习进度。level_id为关卡编号，summary为关卡概要，score为得分(0-100)。"""
    data = _read_progress()
    data["levels"][level_id] = {
        "summary": summary,
        "completed": True,
        "score": score,
        "completed_at": date.today().isoformat(),
    }
    completed = sum(1 for l in data["levels"].values() if l.get("completed"))
    total = sum(l.get("score", 0) for l in data["levels"].values())
    data["completed_levels"] = completed
    data["total_score"] = total
    _write_progress(data)
    return {
        "level_id": level_id,
        "score": score,
        "total_score": total,
        "completed_levels": completed,
        "total_levels": data["total_levels"],
    }
```

- [ ] **Step 9: Run the save_progress tests**

Run: `cd D:/ClaudeCodeWorkSpace/DSPark-Code && uv run pytest tests/test_c_tutor_plugin.py::test_save_progress_creates_record tests/test_c_tutor_plugin.py::test_save_progress_updates_existing_level tests/test_c_tutor_plugin.py::test_save_progress_accumulates_multiple_levels -v`
Expected: 3 PASS

- [ ] **Step 10: Write tests for reset_progress**

Add to `tests/test_c_tutor_plugin.py`:

```python
def test_reset_progress_requires_confirm_yes():
    from skills.c_tutor.plugin import save_progress, reset_progress, load_progress
    save_progress("01", "变量", 80)
    result = reset_progress("no")
    assert "yes" in result.lower()
    data = load_progress()
    assert data["completed_levels"] == 1  # unchanged


def test_reset_progress_clears_all():
    from skills.c_tutor.plugin import save_progress, reset_progress, load_progress
    save_progress("01", "变量", 80)
    save_progress("02", "分支", 90)
    result = reset_progress("yes")
    assert "清空" in result
    data = load_progress()
    assert data["completed_levels"] == 0
    assert data["total_score"] == 0
    assert data["levels"] == {}
```

- [ ] **Step 11: Run the reset_progress tests to verify they fail**

Run: `cd D:/ClaudeCodeWorkSpace/DSPark-Code && uv run pytest tests/test_c_tutor_plugin.py::test_reset_progress_requires_confirm_yes tests/test_c_tutor_plugin.py::test_reset_progress_clears_all -v`
Expected: FAIL (reset_progress not defined)

- [ ] **Step 12: Add reset_progress to plugin.py**

Append to `skills/c-tutor/plugin.py`:

```python
def reset_progress(confirm: str):
    """清空所有闯关学习进度（不可恢复）。confirm 必须为 "yes" 才会执行。"""
    if confirm != "yes":
        return "请将 confirm 参数设为 'yes' 以确认清空全部闯关进度。"
    _write_progress(_default_progress())
    return "所有闯关进度已清空。"
```

- [ ] **Step 13: Run all plugin tests**

Run: `cd D:/ClaudeCodeWorkSpace/DSPark-Code && uv run pytest tests/test_c_tutor_plugin.py -v`
Expected: 6 PASS

- [ ] **Step 14: Add tool decorator fallback for direct imports**

Prepend to `skills/c-tutor/plugin.py` (after the docstring, before function definitions):

```python
# When loaded via SkillLoader.load_plugin(), `tool` is injected into the module
# namespace before exec. For direct imports (tests), provide a no-op fallback.
try:
    tool  # noqa: F823 — injected by SkillLoader
except NameError:
    def tool(func=None, **kwargs):
        """No-op fallback for when plugin is imported outside SkillLoader."""
        if func is None:
            return lambda f: f
        return func
```

Then decorate the three functions with `@tool`:

```python
@tool
def load_progress():
    ...

@tool
def save_progress(level_id: str, summary: str, score: int):
    ...

@tool
def reset_progress(confirm: str):
    ...
```

- [ ] **Step 15: Run all tests again to verify decorator doesn't break anything**

Run: `cd D:/ClaudeCodeWorkSpace/DSPark-Code && uv run pytest tests/test_c_tutor_plugin.py -v`
Expected: 6 PASS

- [ ] **Step 16: Commit**

```bash
cd D:/ClaudeCodeWorkSpace/DSPark-Code && git add skills/c-tutor/plugin.py tests/test_c_tutor_plugin.py && git commit -m "feat: add c-tutor plugin.py with progress management tools

- load_progress: read learning progress from data/c-tutor-progress.json
- save_progress: save level completion with score, recalculate totals
- reset_progress: clear all progress (requires confirm='yes')

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>"
```

---

### Task 2: Create c-tutor SKILL.md with embedded level content

**Files:**
- Create: `skills/c-tutor/SKILL.md`

- [ ] **Step 1: Write skills/c-tutor/SKILL.md**

```markdown
---
name: c-tutor
description: C语言闯关教学——知识点讲解+编程实战，逐关通关学习C语言
---

## 角色切换
进入"C语言导师"模式。你依然是 ATRI，但此时你的核心任务是带领用户系统性地学习 C 语言——先讲解知识点，再出编程题实战考核，逐关通关。

## 行为准则
- 始终以鼓励的语气教学，用户答对了要夸，答错了要耐心指出问题
- 每个知识点的讲解控制在 150-300 字，用生活中的比喻帮助理解
- 编程题必须贴近关卡知识点，不要超纲
- 用户提交代码后，用 read_file 工具读取 workspace 下的 .c 文件进行审查
- 评分后给出具体改进建议，不要只说"不错"或"有问题"
- 禁止帮用户写完整答案，只能给提示和思路
- 用户如果卡住，给一个方向性提示，不要直接给答案

## 闯关规则
- 一共有 10 关，每关一个 C 语言核心知识点，难度递增
- 使用 load_progress 查看当前进度
- 流程：讲解知识点 → 出编程题 → 用户写代码 → 审查评分 → save_progress 记录
- 评分达到 60 分及以上的关卡才算通过
- 通过后才能进入下一关
- 用户可以随时说"复习第X关"来回顾知识点和重新挑战
- 使用 reset_progress("yes") 可以重置全部进度

## 评分标准
| 维度 | 分值 | 说明 |
|------|------|------|
| 正确性 | 60 | 代码逻辑正确，能编译通过并运行出预期结果 |
| 规范性 | 20 | 变量命名清晰、缩进统一、关键处有注释 |
| 效率 | 20 | 算法合理，无明显冗余，无内存泄漏风险 |

## 结束条件
- 用户说"可以了"、"今天先到这里"、"换话题"、"退出学习"等，调用 deactivate_skill("c-tutor") 退出
- 退出时展示当前总进度和得分，鼓励用户下次继续

---

## 关卡列表

> 用户会提供 C 语言学习资料，关卡内容将根据资料调整。以下为默认关卡结构。

### 关卡 01 — Hello World 与程序结构
**知识点：**
- C 程序的基本结构（#include、main 函数、return）
- printf 输出
- 编译和运行的基本概念

**例题：**
编写一个 C 程序，输出以下内容：
```
Hello, C Language!
I am ready to learn.
```

**评分重点：**
- main 函数定义正确
- printf 使用规范，换行符 \n 正确
- 包含 #include <stdio.h>

### 关卡 02 — 变量与数据类型
**知识点：**
- int、float、double、char 类型
- 变量的声明与赋值
- printf 格式化输出（%d、%f、%c）
- scanf 读取用户输入

**例题：**
编写程序，要求用户输入姓名（拼音，不含空格）、年龄和身高（米），然后输出：
```
Name: <姓名>, Age: <年龄>, Height: <身高>m
```

**评分重点：**
- 变量类型选择正确（char 数组、int、float）
- scanf 参数使用 & 取地址符
- 输出格式正确

### 关卡 03 — 运算符与表达式
**知识点：**
- 算术运算符（+、-、*、/、%）
- 关系运算符（>、<、==、!=）
- 逻辑运算符（&&、||、!）
- 运算符优先级
- 类型转换（隐式与显式）

**例题：**
编写程序，输入一个三位整数，输出其各位数字之和。
例如输入 123，输出 6（因为 1+2+3=6）。

**评分重点：**
- 利用 / 和 % 正确拆分各位数字
- 整数除法理解正确

### 关卡 04 — 分支结构
**知识点：**
- if / else if / else
- switch / case / break / default
- 条件表达式嵌套
- 代码缩进规范

**例题：**
编写一个简易计算器，输入两个数和运算符（+、-、*、/），输出计算结果。
需要考虑除数为 0 的情况。

**评分重点：**
- 分支逻辑完整（四个运算符 + 除零处理）
- switch 或 if-else 使用得当
- 边界条件处理

### 关卡 05 — 循环结构
**知识点：**
- for 循环
- while 循环 / do-while 循环
- break 和 continue
- 循环嵌套
- 避免死循环

**例题：**
编写程序输出 9×9 乘法表，要求对齐格式。

**评分重点：**
- 嵌套循环使用正确
- 格式对齐（%2d 或 \t）
- 代码可读性

### 关卡 06 — 数组
**知识点：**
- 一维数组的声明与初始化
- 数组下标（从 0 开始）
- 数组遍历
- 数组越界的危险
- 二维数组基础

**例题：**
输入 10 个整数存入数组，找出最大值、最小值和平均值。

**评分重点：**
- 数组声明和遍历正确
- 最值和平均值的算法正确
- 边界处理（10 个元素全部遍历）

### 关卡 07 — 函数
**知识点：**
- 函数定义与声明（原型）
- 参数传递（值传递）
- 返回值
- 局部变量与作用域
- 递归基础

**例题：**
编写一个函数 `is_prime(int n)` 判断一个数是否为素数，返回 1（是）或 0（否）。
在 main 中调用该函数，输出 1-100 之间的所有素数。

**评分重点：**
- 函数定义和声明正确
- 素数判断逻辑正确（注意边界条件：n<2）
- 函数与 main 配合良好

### 关卡 08 — 指针基础
**知识点：**
- 指针的概念（地址）
- 指针变量的声明（int *p）
- & 取地址和 * 解引用
- 指针与数组的关系
- 指针作为函数参数（模拟引用传递）

**例题：**
编写一个函数 `swap(int *a, int *b)` 交换两个变量的值。在 main 中输入两个数，调用 swap 后输出交换结果。

**评分重点：**
- 指针参数声明正确
- 解引用操作正确
- swap 逻辑正确

### 关卡 09 — 字符串
**知识点：**
- C 风格字符串（char 数组，以 \0 结尾）
- 字符串输入输出（scanf/gets/fgets）
- 常用字符串函数（strlen、strcpy、strcmp、strcat）
- 字符串与指针

**例题：**
输入一个字符串，判断它是否是回文串（正读反读一样，如 "level"）。
忽略大小写差异。

**评分重点：**
- 字符串读取正确（无缓冲区溢出）
- 回文判断算法正确
- 大小写处理

### 关卡 10 — 结构体与文件操作
**知识点：**
- struct 定义与使用
- typedef 简化类型名
- 结构体数组
- 文件读写（fopen、fprintf、fscanf、fclose）
- 文件打开模式（r、w、a）

**例题：**
定义一个学生结构体（姓名、学号、成绩），输入 3 个学生的信息，保存到文件 `students.txt`，
然后从文件中读取并显示所有学生信息。

**评分重点：**
- 结构体定义正确
- 文件打开/关闭配对
- 写入和读取格式一致
- 错误处理（文件打开失败）
```

- [ ] **Step 2: Verify SKILL.md parses correctly by listing skills**

Run: `cd D:/ClaudeCodeWorkSpace/DSPark-Code && uv run python -c "from atri.skill_loader import SkillLoader; skills = SkillLoader.list_skills(); c_tutor = [s for s in skills if s['name'] == 'c-tutor']; print(c_tutor)"`
Expected: `[{'name': 'c-tutor', 'description': 'C语言闯关教学——知识点讲解+编程实战，逐关通关学习C语言'}]`

- [ ] **Step 3: Verify load_skill returns the body (without frontmatter)**

Run: `cd D:/ClaudeCodeWorkSpace/DSPark-Code && uv run python -c "from atri.skill_loader import SkillLoader; body = SkillLoader.load_skill('c-tutor'); print('OK' if body and '角色切换' in body else 'FAIL')"`
Expected: `OK`

- [ ] **Step 4: Commit**

```bash
cd D:/ClaudeCodeWorkSpace/DSPark-Code && git add skills/c-tutor/SKILL.md && git commit -m "feat: add c-tutor SKILL.md with 10-level C learning curriculum

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>"
```

---

### Task 3: Create c-qa SKILL.md

**Files:**
- Create: `skills/c-qa/SKILL.md`

- [ ] **Step 1: Create the skills/c-qa directory**

```bash
mkdir -p D:/ClaudeCodeWorkSpace/DSPark-Code/skills/c-qa
```

- [ ] **Step 2: Write skills/c-qa/SKILL.md**

```markdown
---
name: c-qa
description: C语言答疑模式——解答C语言概念疑问、审查代码、调试排错、最佳实践建议
---

## 角色切换
进入"C语言答疑"模式。你依然是 ATRI，但此时你的核心任务是解答用户提出的所有 C 语言相关问题——不管是概念疑问、代码调试、编译错误，还是最佳实践建议。

## 行为准则

### 概念疑问
- 用通俗的生活比喻解释抽象概念（如"指针就像门牌号"、"内存就像一排带编号的储物柜"）
- 每个概念至少配一个简短的代码示例
- 如果用户追问，逐层深入，不要一下子倒出所有细节
- 鼓励用户动手实验："你可以试着运行这段代码看看效果"

### 代码审查
- 先让用户把代码放到 workspace（粘贴或用 write_file），然后用 read_file 逐行审查
- 先从代码的优点说起，再指出问题
- 每次只指出一个最关键的问题，用户改完再说下一个
- 解释问题时说清楚"为什么这是错的"和"正确做法是什么"
- 禁止帮用户写完整答案，只给修改方向和关键片段

### 编译错误
- 解释编译错误信息的含义，逐条对照代码指出问题位置
- 常见错误直接给出修复方案，罕见错误建议搜索方向
- 注意区分 error（必须修复）和 warning（建议修复）

### 运行结果异常
- 引导用户用 printf 打印中间变量，定位问题
- 帮忙推理逻辑，画流程图辅助理解
- 常见坑：数组越界、未初始化变量、整数溢出、= 和 == 混淆

### 最佳实践
- 指出不好的写法，解释为什么不好（可读性？性能？安全性？）
- 给出改进后的写法并解释好在哪
- 引用 C 标准或业界惯例作为依据

## 知识范围
- C89/C99/C11/C17 标准差异（默认以 C99/C11 为准）
- 基本语法：变量、类型、运算符、控制流、函数
- 指针与内存管理：malloc/free、指针运算、函数指针
- 复合类型：数组、字符串、结构体、联合体、枚举
- 预处理器：宏定义、条件编译、头文件保护
- 文件 I/O、编译链接流程
- 常见未定义行为（UB）：有符号整数溢出、使用未初始化变量、悬垂指针、缓冲区溢出
- 不熟悉的领域直接说"这个我不太确定"，不要瞎编
- 遇到 C++ 独有的特性要明确指出"这是 C++ 的，C 语言里没有这个"

## 交互风格
- 保持专业但不死板，可以用幽默的比喻帮助理解
- 用户明显是新手时，语气更温和、解释更细致
- 用户看起来有经验时，直接切入要点，不要啰嗦
- 代码示例用 ```c ... ``` 格式，不要用纯文本

## 结束条件
- 用户说"可以了"、"换话题"、"没别的问题了"、"谢谢"等，调用 deactivate_skill("c-qa") 退出
- 退出时说"有问题随时问我哦～加油写代码！(●'◡'●)"
```

- [ ] **Step 3: Verify SKILL.md parses correctly**

Run: `cd D:/ClaudeCodeWorkSpace/DSPark-Code && uv run python -c "from atri.skill_loader import SkillLoader; skills = SkillLoader.list_skills(); c_qa = [s for s in skills if s['name'] == 'c-qa']; print(c_qa)"`
Expected: `[{'name': 'c-qa', 'description': 'C语言答疑模式——解答C语言概念疑问、审查代码、调试排错、最佳实践建议'}]`

- [ ] **Step 4: Commit**

```bash
cd D:/ClaudeCodeWorkSpace/DSPark-Code && git add skills/c-qa/SKILL.md && git commit -m "feat: add c-qa SKILL.md for C language Q&A support

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>"
```

---

### Task 4: Integration verification — end-to-end skill activation

**Files:**
- Modify: (none, verification only)

- [ ] **Step 1: Verify both skills appear in skill list**

Run: `cd D:/ClaudeCodeWorkSpace/DSPark-Code && uv run python -c "
from atri.skill_loader import SkillLoader
skills = SkillLoader.list_skills()
names = {s['name'] for s in skills}
assert 'c-tutor' in names, 'c-tutor not found'
assert 'c-qa' in names, 'c-qa not found'
print('Both skills found:', names)
"`

Expected: Both skills found

- [ ] **Step 2: Verify c-tutor plugin loads and tools are extracted**

Run: `cd D:/ClaudeCodeWorkSpace/DSPark-Code && uv run python -c "
from atri.skill_loader import SkillLoader, extract_tools
plugin = SkillLoader.load_plugin('c-tutor')
assert plugin is not None, 'plugin not loaded'
tool_names = [t.function.name for t in extract_tools(plugin)]
assert 'load_progress' in tool_names
assert 'save_progress' in tool_names
assert 'reset_progress' in tool_names
print('All tools registered:', tool_names)
"`

Expected: All tools registered: `['load_progress', 'save_progress', 'reset_progress']`

- [ ] **Step 3: Verify c-qa has no plugin (graceful)**

Run: `cd D:/ClaudeCodeWorkSpace/DSPark-Code && uv run python -c "
from atri.skill_loader import SkillLoader
plugin = SkillLoader.load_plugin('c-qa')
assert plugin is None, 'c-qa should not have plugin.py'
print('c-qa is pure SKILL.md, no plugin — correct')
"`

Expected: `c-qa is pure SKILL.md, no plugin — correct`

- [ ] **Step 4: Verify skill activation via ToolManager**

Run: `cd D:/ClaudeCodeWorkSpace/DSPark-Code && uv run python -c "
from atri.tool_manager import ToolManager
tm = ToolManager()
tm.tool_init()
result = tm.tool_actor('activate_skill', '{\"name\":\"c-tutor\"}', [])
print(result[:200])
"`

Expected: Output contains `[已激活] c-tutor` and `注册工具: load_progress, save_progress, reset_progress`

- [ ] **Step 5: Verify progress file is created on first save**

Run: `cd D:/ClaudeCodeWorkSpace/DSPark-Code && uv run python -c "
from atri import DATA_DIR
from atri.tool_manager import ToolManager
import json

# Clean up any existing progress
pf = DATA_DIR / 'c-tutor-progress.json'
if pf.exists():
    pf.unlink()

tm = ToolManager()
tm.tool_init()
active = []

# Activate skill and save progress
tm.tool_actor('activate_skill', '{\"name\":\"c-tutor\"}', active)
active.append('c-tutor')

result = tm.tool_actor('save_progress', '{\"level_id\":\"01\",\"summary\":\"测试关卡\",\"score\":85}', active)
print('save result:', result)

# Check file
assert pf.exists(), 'Progress file not created'
data = json.loads(pf.read_text(encoding='utf-8'))
assert data['completed_levels'] == 1
assert data['total_score'] == 85
print('Progress file OK:', json.dumps(data, ensure_ascii=False, indent=2))
"`

Expected: Progress file created with correct data

- [ ] **Step 6: Run all tests one final time**

Run: `cd D:/ClaudeCodeWorkSpace/DSPark-Code && uv run pytest tests/test_c_tutor_plugin.py -v`
Expected: 6 PASS

- [ ] **Step 7: Final commit**

```bash
cd D:/ClaudeCodeWorkSpace/DSPark-Code && git add -A && git status
```

Review the staged files, then:

```bash
cd D:/ClaudeCodeWorkSpace/DSPark-Code && git commit -m "feat: add c-tutor and c-qa skills for C language learning

c-tutor: 10-level gamified C learning with progress tracking plugin
c-qa: C language Q&A, code review, and debugging support

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>"
```
