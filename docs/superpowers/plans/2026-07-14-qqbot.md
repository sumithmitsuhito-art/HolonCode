# QQ Bot 接入实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 在 DSPark-Code 桌面端接入 QQ Bot，用户可通过 QQ 与小洛对话，消息自动同步到桌面端 QQ 专用会话。

**Architecture:** 新建 `src/qqbot/` 独立模块（不混入 atri），使用腾讯官方 QQ Bot API v2。WebSocket 接收事件 + REST 发送消息，QThread 运行 asyncio 事件循环，通过 Qt 信号与 AppShell 通信。

**Tech Stack:** aiohttp（WebSocket）、httpx（已有）、asyncio + QThread

---

### Task 1: 配置模块 `src/qqbot/config.py`

**Files:**
- Create: `src/qqbot/__init__.py`
- Create: `src/qqbot/config.py`
- Test: `tests/test_qqbot_config.py`

- [ ] **Step 1: 编写失败测试**

```python
import json
import pytest
from pathlib import Path
from atri import DATA_DIR

@pytest.fixture
def clean_settings():
    settings_path = DATA_DIR / "UserSettings.json"
    backup = None
    if settings_path.exists():
        backup = settings_path.read_bytes()
    yield
    if backup is not None:
        settings_path.write_bytes(backup)
    elif settings_path.exists():
        settings_path.unlink()


def test_load_empty_config(clean_settings):
    from src.qqbot.config import QQBotConfig
    settings_path = DATA_DIR / "UserSettings.json"
    if settings_path.exists():
        settings_path.unlink()
    cfg = QQBotConfig.load()
    assert cfg.app_id == ""
    assert cfg.client_secret == ""


def test_load_from_file(clean_settings):
    from src.qqbot.config import QQBotConfig
    DATA_DIR.mkdir(parents=True, exist_ok=True)
    (DATA_DIR / "UserSettings.json").write_text(json.dumps({
        "QQBot": {"AppId": "test-app-id", "ClientSecret": "test-secret"}
    }), encoding="utf-8")
    cfg = QQBotConfig.load()
    assert cfg.app_id == "test-app-id"
    assert cfg.client_secret == "test-secret"


def test_load_from_env(clean_settings, monkeypatch):
    from src.qqbot.config import QQBotConfig
    settings_path = DATA_DIR / "UserSettings.json"
    if settings_path.exists():
        settings_path.unlink()
    monkeypatch.setenv("QQ_APP_ID", "env-app-id")
    monkeypatch.setenv("QQ_CLIENT_SECRET", "env-secret")
    cfg = QQBotConfig.load()
    assert cfg.app_id == "env-app-id"
    assert cfg.client_secret == "env-secret"


def test_is_configured():
    from src.qqbot.config import QQBotConfig
    assert not QQBotConfig("", "").is_configured()
    assert QQBotConfig("id", "secret").is_configured()
```

- [ ] **Step 2: 运行测试验证失败**

Run: `pytest tests/test_qqbot_config.py -v`
Expected: 4 FAIL (import errors)

- [ ] **Step 3: 创建空包文件**

```python
# src/qqbot/__init__.py
```
（空文件）

- [ ] **Step 4: 实现配置模块**

```python
# src/qqbot/config.py
import json
import os
from dataclasses import dataclass
from atri import DATA_DIR


@dataclass
class QQBotConfig:
    app_id: str
    client_secret: str

    def is_configured(self) -> bool:
        return bool(self.app_id and self.client_secret)

    @classmethod
    def load(cls) -> "QQBotConfig":
        config_path = DATA_DIR / "UserSettings.json"
        file_cfg = {}
        if config_path.exists():
            try:
                data = json.loads(config_path.read_text(encoding="utf-8"))
                file_cfg = data.get("QQBot", {})
            except (json.JSONDecodeError, OSError):
                pass
        return cls(
            app_id=file_cfg.get("AppId", "") or os.getenv("QQ_APP_ID", ""),
            client_secret=file_cfg.get("ClientSecret", "") or os.getenv("QQ_CLIENT_SECRET", ""),
        )
```

- [ ] **Step 5: 运行测试验证通过**

Run: `pytest tests/test_qqbot_config.py -v`
Expected: 4 PASS

- [ ] **Step 6: 提交**

```bash
git add src/qqbot/__init__.py src/qqbot/config.py tests/test_qqbot_config.py
git commit -m "feat(qqbot): add QQ Bot config module"
```

---

### Task 2: WebSocket 连接模块 `src/qqbot/connection.py`

**Files:**
- Create: `src/qqbot/connection.py`
- Test: `tests/test_qqbot_connection.py`

- [ ] **Step 1: 编写连接模块测试**

```python
import json
import pytest
from unittest.mock import AsyncMock, patch, MagicMock


class TestPayloadDispatch:
    """Test _dispatch_payload without real WebSocket."""

    @pytest.fixture
    def adapter(self):
        from src.qqbot.connection import QQBotConnection
        conn = QQBotConnection.__new__(QQBotConnection)
        conn._log_tag = "test"
        conn._session_id = None
        conn._last_seq = None
        conn._heartbeat_interval = 30
        conn._ws = None
        conn._running = True
        conn._message_callback = None
        conn._heartbeat_task = None
        conn._connect_lock = None
        return conn

    def test_hello_wo_session_sends_identify(self, adapter):
        adapter._send_identify = AsyncMock()
        adapter._send_resume = AsyncMock()
        adapter._create_task = lambda coro: None  # no-op in tests
        adapter._dispatch_payload({"op": 10, "d": {"heartbeat_interval": 30000}})
        # heartbeat_interval should be set to 80%
        assert adapter._heartbeat_interval == 24.0

    def test_hello_with_session_sends_resume(self, adapter):
        adapter._session_id = "abc"
        adapter._last_seq = 5
        adapter._send_identify = AsyncMock()
        adapter._send_resume = AsyncMock()
        adapter._create_task = lambda coro: None
        adapter._dispatch_payload({"op": 10, "d": {"heartbeat_interval": 30000}})

    def test_ready_stores_session_id(self, adapter):
        adapter._dispatch_payload({"op": 0, "t": "READY", "d": {"session_id": "sess-123"}})
        assert adapter._session_id == "sess-123"

    def test_invalid_session_clears_session(self, adapter):
        adapter._session_id = "abc"
        adapter._dispatch_payload({"op": 9, "d": False})
        assert adapter._session_id is None
        assert adapter._last_seq is None

    def test_dispatch_sequence_update(self, adapter):
        adapter._dispatch_payload({"op": 0, "t": "C2C_MESSAGE_CREATE", "s": 42, "d": {"id": "1"}})
        assert adapter._last_seq == 42

    def test_close_code_is_fatal(self):
        from src.qqbot.connection import _is_fatal_close_code
        assert _is_fatal_close_code(4001) is True
        assert _is_fatal_close_code(4004) is False  # token expired, retryable
        assert _is_fatal_close_code(4008) is False  # rate limited, retryable
```

- [ ] **Step 2: 运行测试验证失败**

Run: `pytest tests/test_qqbot_connection.py -v`
Expected: FAIL

- [ ] **Step 3: 实现连接模块**

```python
# src/qqbot/connection.py
import asyncio
import json
import logging
import time

logger = logging.getLogger(__name__)

# ── QQ Bot API constants ──────────────────────────────────────────────
API_BASE = "https://api.sgroup.qq.com"
TOKEN_URL = "https://bots.qq.com/app/getAppAccessToken"
GATEWAY_URL_PATH = "/gateway"

# Fatal close codes — stop reconnecting
FATAL_CLOSE_CODES = {4001, 4002, 4010, 4011, 4012, 4013, 4014, 4914, 4915}
# Token expired — retryable
TOKEN_EXPIRED_CODES = {4004}
# Rate limited
RATE_LIMIT_CODES = {4008}
# Session invalid — retryable
SESSION_INVALID_CODES = {4006, 4007, 4009}

RECONNECT_BACKOFF = [2, 5, 10, 30, 60]
MAX_RECONNECT_ATTEMPTS = 100
RECONNECT_RESET_AFTER = 60  # reset backoff counter after 60s of stable connection


def _is_fatal_close_code(code: int) -> bool:
    return code in FATAL_CLOSE_CODES


class QQCloseError(Exception):
    def __init__(self, code: int, reason: str = ""):
        self.code = code
        self.reason = reason
        super().__init__(f"WebSocket closed (code={code}, reason={reason})")


class QQBotConnection:
    """WebSocket gateway client for QQ Bot API v2.
    
    Handles: connect → Hello → Identify/Resume → listen loop +
    heartbeat → reconnect on close.
    
    Call ``set_message_callback(coro_fn)`` to receive parsed message dicts.
    """

    def __init__(self, app_id: str, client_secret: str):
        import aiohttp
        self._app_id = app_id
        self._client_secret = client_secret
        self._log_tag = f"qqbot:{app_id[:8]}"
        self._session: aiohttp.ClientSession | None = None
        self._ws: aiohttp.ClientWebSocketResponse | None = None
        self._token: str | None = None
        self._token_expiry: float = 0
        self._session_id: str | None = None
        self._last_seq: int | None = None
        self._heartbeat_interval: float = 30
        self._running: bool = False
        self._message_callback = None
        self._heartbeat_task: asyncio.Task | None = None
        self._reconnect_count: int = 0
        self._last_connect_time: float = 0
        self._connect_lock = asyncio.Lock()

    def set_message_callback(self, callback):
        """Set async callback(event_type, payload) for inbound messages."""
        self._message_callback = callback

    async def _ensure_token(self) -> str:
        import httpx
        now = time.time()
        if self._token and now < self._token_expiry - 60:
            return self._token
        async with httpx.AsyncClient(timeout=30) as client:
            resp = await client.post(TOKEN_URL, json={
                "appId": self._app_id,
                "clientSecret": self._client_secret,
            })
            resp.raise_for_status()
            data = resp.json()
            self._token = data["access_token"]
            # Default TTL 7200s
            self._token_expiry = now + data.get("expires_in", 7200)
            return self._token

    async def _get_gateway_url(self) -> str:
        import httpx
        token = await self._ensure_token()
        async with httpx.AsyncClient(timeout=30) as client:
            resp = await client.get(
                API_BASE + GATEWAY_URL_PATH,
                headers={"Authorization": f"QQBot {token}"},
            )
            resp.raise_for_status()
            return resp.json()["url"]

    async def connect(self) -> None:
        """Connect and run the main event loop (blocks until stop())."""
        import aiohttp
        self._running = True
        self._session = aiohttp.ClientSession()
        try:
            while self._running:
                try:
                    ws_url = await self._get_gateway_url()
                    async with self._session.ws_connect(ws_url) as ws:
                        self._ws = ws
                        self._heartbeat_task = asyncio.create_task(
                            self._heartbeat_loop()
                        )
                        await self._read_events()
                except QQCloseError as e:
                    if _is_fatal_close_code(e.code):
                        logger.error("[%s] Fatal close code %s, stopping", self._log_tag, e.code)
                        break
                    if e.code in TOKEN_EXPIRED_CODES:
                        self._token = None
                    if e.code in SESSION_INVALID_CODES:
                        self._session_id = None
                        self._last_seq = None
                    if e.code in RATE_LIMIT_CODES:
                        await asyncio.sleep(60)
                except Exception as e:
                    logger.debug("[%s] Connection error: %s", self._log_tag, e)
                if self._running:
                    await self._reconnect_delay()
        finally:
            if self._session:
                await self._session.close()

    async def stop(self) -> None:
        self._running = False
        if self._heartbeat_task:
            self._heartbeat_task.cancel()
        if self._ws and not self._ws.closed:
            await self._ws.close()

    async def _read_events(self) -> None:
        import aiohttp
        while self._running:
            msg = await self._ws.receive()
            if msg.type == aiohttp.WSMsgType.TEXT:
                payload = json.loads(msg.data)
                self._dispatch_payload(payload)
            elif msg.type == aiohttp.WSMsgType.CLOSE:
                raise QQCloseError(msg.data, msg.extra)
            elif msg.type in {aiohttp.WSMsgType.CLOSED, aiohttp.WSMsgType.ERROR}:
                raise RuntimeError("WebSocket closed")

    async def _heartbeat_loop(self) -> None:
        try:
            while self._running:
                await asyncio.sleep(self._heartbeat_interval)
                if self._ws and not self._ws.closed:
                    await self._ws.send_json({"op": 1, "d": self._last_seq})
        except asyncio.CancelledError:
            pass

    async def _send_identify(self) -> None:
        token = await self._ensure_token()
        payload = {
            "op": 2,
            "d": {
                "token": f"QQBot {token}",
                "intents": (1 << 25) | (1 << 12),  # C2C + GROUP_AT + DIRECT_MESSAGE
                "shard": [0, 1],
            },
        }
        if self._ws and not self._ws.closed:
            await self._ws.send_json(payload)

    async def _send_resume(self) -> None:
        token = await self._ensure_token()
        payload = {
            "op": 6,
            "d": {"token": f"QQBot {token}", "session_id": self._session_id, "seq": self._last_seq},
        }
        if self._ws and not self._ws.closed:
            await self._ws.send_json(payload)
            logger.info("[%s] Resume sent", self._log_tag)

    def _dispatch_payload(self, payload: dict) -> None:
        op = payload.get("op")
        t = payload.get("t")
        s = payload.get("s")
        d = payload.get("d")
        if isinstance(s, int) and (self._last_seq is None or s > self._last_seq):
            self._last_seq = s

        if op == 10:
            d_data = d if isinstance(d, dict) else {}
            interval_ms = d_data.get("heartbeat_interval", 30000)
            self._heartbeat_interval = interval_ms / 1000.0 * 0.8
            if self._session_id and self._last_seq is not None:
                self._create_task(self._send_resume())
            else:
                self._create_task(self._send_identify())
        elif op == 0 and t:
            if t == "READY":
                if isinstance(d, dict):
                    self._session_id = d.get("session_id")
            elif t in ("C2C_MESSAGE_CREATE", "GROUP_AT_MESSAGE_CREATE"):
                if self._message_callback:
                    self._create_task(self._message_callback(t, d))
        elif op == 7:
            if self._ws and not self._ws.closed:
                self._create_task(self._ws.close())
        elif op == 9:
            resumable = bool(d) if d is not None else False
            if not resumable:
                self._session_id = None
                self._last_seq = None
            if self._ws and not self._ws.closed:
                self._create_task(self._ws.close())

    def _create_task(self, coro):
        try:
            loop = asyncio.get_running_loop()
            return loop.create_task(coro)
        except RuntimeError:
            return None

    async def _reconnect_delay(self) -> None:
        now = time.time()
        if now - self._last_connect_time > RECONNECT_RESET_AFTER:
            self._reconnect_count = 0
        delay = RECONNECT_BACKOFF[min(self._reconnect_count, len(RECONNECT_BACKOFF) - 1)]
        self._reconnect_count = min(self._reconnect_count + 1, MAX_RECONNECT_ATTEMPTS - 1)
        self._last_connect_time = now
        await asyncio.sleep(delay)
```

- [ ] **Step 4: 运行测试验证通过**

Run: `pytest tests/test_qqbot_connection.py -v`
Expected: all PASS

- [ ] **Step 5: 提交**

```bash
git add src/qqbot/connection.py tests/test_qqbot_connection.py
git commit -m "feat(qqbot): add WebSocket connection module"
```

---

### Task 3: 消息处理模块 `src/qqbot/handler.py`

**Files:**
- Create: `src/qqbot/handler.py`
- Test: `tests/test_qqbot_handler.py`

- [ ] **Step 1: 编写消息处理测试**

```python
import pytest


class TestMessageParsing:

    def test_parse_c2c_message(self):
        from src.qqbot.handler import _parse_inbound
        payload = {
            "id": "msg-001",
            "author": {"id": "user-openid-123", "username": "测试用户"},
            "content": "你好小洛",
        }
        result = _parse_inbound("C2C_MESSAGE_CREATE", payload)
        assert result is not None
        assert result["chat_type"] == "c2c"
        assert result["author_id"] == "user-openid-123"
        assert result["author_name"] == "测试用户"
        assert result["content"] == "你好小洛"
        assert result["message_id"] == "msg-001"

    def test_parse_group_message_strips_mention(self):
        from src.qqbot.handler import _parse_inbound
        payload = {
            "id": "msg-002",
            "author": {"id": "user-456"},
            "content": "<@!bot-id> 你好",
            "group_openid": "group-789",
        }
        result = _parse_inbound("GROUP_AT_MESSAGE_CREATE", payload)
        assert result["chat_type"] == "group"
        assert result["group_openid"] == "group-789"
        # Content should be stripped of @mention
        assert "你好" in result["content"]


class TestAccessControl:

    def test_dm_policy_disabled(self):
        from src.qqbot.handler import _check_dm_policy
        assert not _check_dm_policy("disabled", [], "user-1")

    def test_dm_policy_open(self):
        from src.qqbot.handler import _check_dm_policy
        assert _check_dm_policy("open", [], "user-1")

    def test_dm_policy_allowlist_match(self):
        from src.qqbot.handler import _check_dm_policy
        assert _check_dm_policy("allowlist", ["user-1", "user-2"], "user-1")

    def test_dm_policy_allowlist_no_match(self):
        from src.qqbot.handler import _check_dm_policy
        assert not _check_dm_policy("allowlist", ["user-1"], "user-3")


class TestMentionStripping:

    def test_strip_at_mention(self):
        from src.qqbot.handler import _strip_mention
        assert _strip_mention("<@!bot123> 你好世界") == "你好世界"
        assert _strip_mention("你好") == "你好"

    def test_strip_leading_whitespace(self):
        from src.qqbot.handler import _strip_mention
        assert _strip_mention("  你好世界") == "你好世界"
```

- [ ] **Step 2: 运行测试验证失败**

Run: `pytest tests/test_qqbot_handler.py -v`
Expected: FAIL

- [ ] **Step 3: 实现消息处理模块**

```python
# src/qqbot/handler.py
import logging
import re
from datetime import datetime

logger = logging.getLogger(__name__)

# ── Constants ─────────────────────────────────────────────────────────
API_BASE = "https://api.sgroup.qq.com"
_AT_RE = re.compile(r"<@!\w+>\s*")


def _strip_mention(text: str) -> str:
    return _AT_RE.sub("", text).strip()


def _parse_inbound(event_type: str, payload: dict) -> dict | None:
    """Parse a QQ message payload into a normalized dict. Returns None if unparseable."""
    msg_id = payload.get("id")
    author = payload.get("author", {}) if isinstance(payload.get("author"), dict) else {}
    author_id = author.get("id", "")
    author_name = author.get("username", "")
    content = payload.get("content", "")

    if event_type == "C2C_MESSAGE_CREATE":
        return {
            "chat_type": "c2c",
            "message_id": msg_id,
            "author_id": author_id,
            "author_name": author_name,
            "content": content,
        }
    elif event_type == "GROUP_AT_MESSAGE_CREATE":
        return {
            "chat_type": "group",
            "message_id": msg_id,
            "author_id": author_id,
            "author_name": author_name,
            "content": _strip_mention(content),
            "group_openid": payload.get("group_openid", ""),
        }
    return None


def _check_dm_policy(policy: str, allowlist: list[str], author_id: str) -> bool:
    if policy == "disabled":
        return False
    elif policy == "allowlist":
        return author_id in allowlist
    return True  # "open"


class QQBotHandler:
    """Process inbound QQ messages: ACL check → AI reply → send via REST."""

    def __init__(self, app_id: str, client_secret: str, ai_service, session_factory):
        self._app_id = app_id
        self._client_secret = client_secret
        self._ai_service = ai_service
        self._session_factory = session_factory  # callable that returns the QQ session
        self._dm_policy = "open"
        self._allowlist: list[str] = []
        self._seen_ids: dict[str, float] = {}  # dedup

    async def handle_message(self, event_type: str, payload: dict) -> None:
        parsed = _parse_inbound(event_type, payload)
        if parsed is None:
            return

        # Dedup
        msg_id = parsed["message_id"]
        now = datetime.now().timestamp()
        if msg_id in self._seen_ids:
            if now - self._seen_ids[msg_id] < 300:
                return
        self._seen_ids[msg_id] = now
        if len(self._seen_ids) > 1000:
            self._seen_ids.clear()

        # ACL
        author_id = parsed["author_id"]
        if parsed["chat_type"] == "c2c":
            if not _check_dm_policy(self._dm_policy, self._allowlist, author_id):
                return

        content = parsed["content"].strip()
        if not content:
            return

        # Get or create QQ session
        session_id = self._session_factory()

        # Call AI
        await self._ai_reply(parsed, content, session_id)

    async def _ai_reply(self, parsed: dict, content: str, session_id: str) -> None:
        """Stream AI response and send to QQ."""
        # Switch to QQ session context
        old_session = self._ai_service.conversation.session_id
        self._ai_service.conversation.session_id = session_id
        if old_session != session_id:
            self._ai_service.conversation.history.clear()
            self._ai_service.conversation.content_init()

        try:
            full_reply = ""
            async for event in self._ai_service.ai_chat(content):
                if event.type == "content":
                    full_reply += event.text
                elif event.type == "done":
                    pass  # Reply already accumulated
            if full_reply:
                await self._send_reply(parsed, full_reply)
        finally:
            # Restore original session
            if old_session != session_id:
                self._ai_service.conversation.save_history()
            self._ai_service.conversation.session_id = old_session
            if old_session:
                self._ai_service.conversation.history.clear()
                self._ai_service.conversation.content_init()

    async def _send_reply(self, parsed: dict, text: str) -> None:
        """Send text reply via QQ REST API."""
        import httpx
        token = await self._get_token()
        headers = {"Authorization": f"QQBot {token}", "Content-Type": "application/json"}

        if parsed["chat_type"] == "c2c":
            url = f"{API_BASE}/v2/users/{parsed['author_id']}/messages"
            body = {"content": text, "msg_type": 0, "msg_id": parsed["message_id"]}
        else:
            url = f"{API_BASE}/v2/groups/{parsed['group_openid']}/messages"
            body = {"content": text, "msg_type": 0, "msg_id": parsed["message_id"]}

        async with httpx.AsyncClient(timeout=30) as client:
            resp = await client.post(url, headers=headers, json=body)
            resp.raise_for_status()

    async def _get_token(self) -> str:
        import httpx
        import time
        if hasattr(self, "_token") and hasattr(self, "_token_expiry"):
            if time.time() < self._token_expiry - 60:
                return self._token
        async with httpx.AsyncClient(timeout=30) as client:
            resp = await client.post(
                "https://bots.qq.com/app/getAppAccessToken",
                json={"appId": self._app_id, "clientSecret": self._client_secret},
            )
            resp.raise_for_status()
            data = resp.json()
            self._token = data["access_token"]
            self._token_expiry = time.time() + data.get("expires_in", 7200)
            return self._token
```

- [ ] **Step 4: 运行测试验证通过**

Run: `pytest tests/test_qqbot_handler.py -v`
Expected: all PASS

- [ ] **Step 5: 提交**

```bash
git add src/qqbot/handler.py tests/test_qqbot_handler.py
git commit -m "feat(qqbot): add message handler with ACL and AI integration"
```

---

### Task 4: QQ Bot 运行器 `src/qqbot/__init__.py`

**Files:**
- Modify: `src/qqbot/__init__.py`

- [ ] **Step 1: 实现 QQBotRunner（QThread + asyncio）**

```python
# src/qqbot/__init__.py
import asyncio
import logging
from PySide6.QtCore import QThread, Signal
from .config import QQBotConfig
from .connection import QQBotConnection
from .handler import QQBotHandler

logger = logging.getLogger(__name__)

QQ_SESSION_TITLE = "[QQ] QQ聊天"


class QQBotRunner(QThread):
    """QThread that runs the QQ Bot asyncio event loop.
    
    Emits signals for AppShell to update the QQ session UI.
    """
    message_received = Signal(str, str)  # (author_name, content)
    reply_sent = Signal(str)  # (reply_text)

    def __init__(self, ai_service, get_qq_session_id, parent=None):
        super().__init__(parent)
        self._ai_service = ai_service
        self._get_qq_session_id = get_qq_session_id  # callable → str
        self._connection: QQBotConnection | None = None
        self._handler: QQBotHandler | None = None

    def run(self):
        cfg = QQBotConfig.load()
        if not cfg.is_configured():
            logger.info("QQ Bot not configured, skipping")
            return

        self._connection = QQBotConnection(cfg.app_id, cfg.client_secret)
        self._handler = QQBotHandler(
            cfg.app_id, cfg.client_secret,
            self._ai_service,
            self._get_qq_session_id,
        )

        async def on_message(event_type: str, payload: dict):
            # Signal UI with parsed message info
            parsed = self._parse_for_ui(event_type, payload)
            if parsed:
                self.message_received.emit(parsed["author_name"], parsed["content"])
            await self._handler.handle_message(event_type, payload)

        self._connection.set_message_callback(on_message)

        loop = asyncio.new_event_loop()
        asyncio.set_event_loop(loop)
        try:
            loop.run_until_complete(self._connection.connect())
        finally:
            loop.run_until_complete(loop.shutdown_asyncgens())
            loop.close()

    def stop_bot(self):
        if self._connection:
            # Schedule stop in the running event loop
            loop = asyncio.get_event_loop()
            loop.create_task(self._connection.stop())
        self.quit()
        self.wait(5000)

    @staticmethod
    def _parse_for_ui(event_type: str, payload: dict) -> dict | None:
        from .handler import _parse_inbound
        return _parse_inbound(event_type, payload)
```

- [ ] **Step 2: 运行已有测试验证无回归**

Run: `pytest tests/test_qqbot_config.py tests/test_qqbot_connection.py tests/test_qqbot_handler.py -v`
Expected: all PASS

- [ ] **Step 3: 提交**

```bash
git add src/qqbot/__init__.py
git commit -m "feat(qqbot): add QQBotRunner QThread for background bot operation"
```

---

### Task 5: AppShell 集成

**Files:**
- Modify: `src/atri/ui/app_shell.py`

- [ ] **Step 1: 在 AppShell 中加入 QQ Bot 生命周期**

在 `__init__` 末尾添加：

```python
# --- QQ Bot ---
from atri.ui.sidebar import list_sessions, create_session, get_session_by_title

def _find_or_create_qq_session() -> str:
    """Find existing QQ session or create one."""
    sessions = list_sessions()
    for s in sessions:
        if "[QQ]" in s.get("title", ""):
            return s["id"]
    sid = create_session("[QQ] QQ聊天")
    return sid

self._qq_session_id = _find_or_create_qq_session()
from src.qqbot import QQBotRunner
self._qq_bot = QQBotRunner(
    self.ai_service,
    lambda: self._qq_session_id,
)
self._qq_bot.message_received.connect(self._on_qq_message)
self._qq_bot.start()
```

添加信号处理方法：

```python
def _on_qq_message(self, author_name: str, content: str):
    """QQ message received → ensure QQ session visible and append message."""
    # If user is looking at another session, don't switch but mark as unread
    # For now, just make sure the session exists and has content
    pass  # The AI reply is streamed via ai_service and saved to session file
```

在 `closeEvent` 中添加：

```python
if self._qq_bot and self._qq_bot.isRunning():
    self._qq_bot.stop_bot()
```

- [ ] **Step 2: 手动启动桌面端验证**

Run: `uv run atri-ui`
Expected: 首次启动自动创建 "[QQ] QQ聊天" 会话，若未配置 QQ Bot 则静默跳过

- [ ] **Step 3: 提交**

```bash
git add src/atri/ui/app_shell.py
git commit -m "feat(qqbot): integrate QQ Bot into AppShell"
```

---

### Task 6: 补充依赖配置

**Files:**
- Modify: `pyproject.toml`

- [ ] **Step 1: 添加 aiohttp 依赖**

在 `pyproject.toml` 的 dependencies 中添加 `aiohttp`。编辑方式：在现有依赖列表添加 `"aiohttp",`。

- [ ] **Step 2: 安装依赖**

Run: `uv pip install aiohttp`

- [ ] **Step 3: 提交**

```bash
git add pyproject.toml
git commit -m "chore: add aiohttp dependency for QQ Bot WebSocket"
```
